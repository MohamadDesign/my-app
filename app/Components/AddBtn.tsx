"use client";
import React from "react";

const AddBtn = () => {
  return (
    <>
      <button onClick={() => console.log("add to click")}>Add to card</button>
    </>
  );
};

export default AddBtn;
